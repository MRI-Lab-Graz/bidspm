[pytest]
# local_src/ vendors the upstream bidspm-local project with its own
# self-contained test suite (own pyproject.toml/tox.ini); it is not
# importable from the repo root and must not be collected here.
testpaths = tests
norecursedirs = .git .bidspm external containers local_src static templates docs
